<resources>
    <!-- Base application theme. -->
    <style name="Theme.MapApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="BottomNavThem" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@android:color/black</item>
    </style>

    <style name="RounderBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <!--        <item name="android:background">@android:color/transparent</item>-->
        <!--        <item name="android:windowIsFloating">false</item>-->
        <!--        <item name="android:windowSoftInputMode">adjustResize</item>-->

        <item name="behavior_skipCollapsed">true</item>
        <item name="behavior_fitToContents">true</item>
        <item name="behavior_peekHeight">1000dp</item>
        // yep, that helped to skip collapsed state at initial
        <item name="behavior_hideable">true</item>

        <item name="android:background">@drawable/background_card</item>

    </style>

    <style name="RounderBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/RounderBottomSheetStyle</item>
    </style>

    <style name="SearchBarStyle" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@drawable/rectangle</item>
        <item name="submitBackground">@android:color/black</item>
        <item name="android:imeOptions">actionSearch</item>
        <item name="android:textColorHint">@color/grey_dark</item>
        <item name="colorControlActivated">@android:color/black</item>
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="android:textColorSecondary">@android:color/black</item>
        <item name="searchHintIcon">@drawable/ic_baseline_search_24</item>
        <!--        <item name="searchHintIcon">@drawable/ic_ab_small_search</item>-->
        <item name="defaultQueryHint">@string/search</item>
        <item name="closeIcon">@drawable/ic_close</item>
        <item name="iconifiedByDefault">false</item>

    </style>

    <style name="Theme_Dialog" parent="Theme.MapApp">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
    </style>


</resources>